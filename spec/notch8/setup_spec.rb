require 'spec_helper'

describe Notch8::Setup do
  it 'has a version number' do
    expect(Notch8::Setup::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
